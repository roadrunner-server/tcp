version: '3'

server:
  command: "php php_test_files/psr-worker-tcp.php"

tcp:
  servers:
    server1:
      addr: "garbage://127.0.0.1:7792"
      delimiter: "\r\n"

  pool:
    num_workers: 1
    allocate_timeout: 60s

endure:
  grace_period: 10s
